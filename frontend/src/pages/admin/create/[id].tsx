import React, { ReactElement } from "react";
import ChangePageComp from "@/shared/pages/Admin//ChangePage";

const ChangePage = (): ReactElement => {
    return <ChangePageComp />;
};

export default ChangePage;
